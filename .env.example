# NEXT_PUBLIC_PRIVATE_KEY=
# Chave privada da carteira usada pelo trading automático REAL (Arc Testnet).
# Deve corresponder a uma carteira com USDC faucet na Arc Testnet.
# NUNCA commite este arquivo com valor real — use apenas .env.local (gitignored).
